ifeval::["cx-manager"=="cx-manager"]
:cx-manager:
endif::[]
ifeval::["cx-manager"=="data-analyst"]
:data-analyst:
endif::[]
ifeval::["cx-manager"=="digital-marketer"]
:digital-marketer:
endif::[]
ifeval::["cx-manager"=="director-of-commerce"]
:director-of-commerce:
endif::[]
ifeval::["cx-manager"=="merchandiser"]
:merchandiser:
endif::[]
ifeval::["cx-manager"=="product-manager"]
:product-manager:
endif::[]
